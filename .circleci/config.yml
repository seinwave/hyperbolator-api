version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-test:
    executor:
      name: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
            - run: npm test
    hi-there:
      - checkout
      - run: echo "hi meepis"
workflows:
    build-and-test:
      jobs:
        - hi-there 
        - build-and-test:
          requires:
            - hi-there